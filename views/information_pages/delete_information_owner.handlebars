<div style="margin-left: 200px; margin-right: 200px; font-size: 13px">
    <br>
    <h2>Owner</h2>
    <br>
    <p style="font-size: 20px; font-weight: 600">Delete</p>
    <br>

    {{!-- On One Line Form- --}}
    <div class="formDiv">
        <form data-id="{{this.id}}" class="update-form">
            <div class="form-group-sm row">
                <div class="col-sm">
                    <label for="ex1">NO</label>
                    <input class="form-control form-control-sm" placeholder="NO" name="NO" id="NO"
                        value="{{this.information_owner_no}}" style="width: 200px" disabled>
                    <br>
                    <label for="ex2">Name</label>
                    <input class="form-control form-control-sm" placeholder="Name" name="Name" id="div_name"
                        value="{{this.information_owner_name}}" style="width: 200px" disabled>
                    <br>
                    <label for="ex1">Address</label>
                    <input class="form-control form-control-sm" placeholder="Address" name="Address" id="Address"
                        value="{{this.information_owner_address}}" style="width: 200px" disabled>
                    <br>
                    <label for="ex2">Tel</label>
                    <input class="form-control form-control-sm" placeholder="Tel" name="Tel" id="Tel"
                        value="{{this.information_owner_tel}}" style="width: 200px" disabled>
                    <br>
                    
                    {{!-- <label for="ex2">Name</label>
          <input class="form-control form-control-sm" placeholder="Name" name="Name" id="Name" maxlength="45" required
            style="text-transform: uppercase; width: 100px">
          <br> --}}
                    <button type="submit" class="btn btn-secondary btn-sm" style="margin-top: 25px">Delete</button>
                </div>

            </div>
        </form>
        <button class="btn btn-secondary btn-sm" style="margin-top: 25px"
            onclick="window.history.back()">Cancel</button>
        {{!-- <button class="btn btn-secondary btn-sm" style="margin-top: 25px"
        onclick="window.location.href='/master_division'">Cancel</button> --}}
        <br>
    </div>

</div>
<br>


<script type="text/javascript">

    $(".update-form").on("submit", function (event) {
        // Make sure to preventDefault on a submit event.
        event.preventDefault();


        var id = $(this).data("id");
        // Send the PUT request.
        $.ajax("/information_owner_list/" + id, {
            type: "DELETE",

        }).then(
            function () {
                console.log("updated!");
                // Reload the page to get the updated list
                location.assign("/information_owner");
            }
        );
    });

    function goBack() {
        window.history.back();
    }

</script>